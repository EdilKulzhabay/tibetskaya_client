# Инструкция по созданию IPA файла для iOS

## 1. Подготовка к сборке

### Установка зависимостей
```bash
cd ios
pod install
cd ..
```

### Очистка кэша (если нужно)
```bash
# Очистка React Native кэша
npx react-native start --reset-cache

нужно изменить версию в двух местах в файле 
ios/tibetskayaClientApp.xcodeproj/project.pbxproj

удалить старый ipa в 
build/ipa

# Очистка iOS кэша
rm -rf ios/build
rm -rf ~/Library/Developer/Xcode/DerivedData/tibetskayaClientApp-*

xcodebuild -exportArchive -archivePath build/tibetskayaClientApp.xcarchive -exportPath build/ipa -exportOptionsPlist build/ipa/ExportOptions.plist